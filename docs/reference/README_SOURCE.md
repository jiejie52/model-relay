# Source basis

This package was produced from the user-provided baseline `model-relay_0903` source archive and the following design inputs bundled here for traceability:

- `Model_Relay_技术架构与恢复机制说明_20260904_双层恢复修订版.docx`
- `Relay改造方案.md`

`SOURCE_DIFF_KIMI_V2.patch` at the package root contains a recursive diff from the extracted baseline source to this package (generated files such as `__pycache__` and bundled reference documents are excluded).
