Model Relay V2
Build: 2.0.4-hotfix4
Date: 2026-09-18
Reason: Railway proved the hotfix3 Dockerfile/BUILD_INFO were deployed, but the build-context requirements.txt was an older mixed-version copy without boto3/botocore. The diagnostic grep then exited 1 before the explicit boto install step could run.
Primary fix: make the requirements diagnostic non-fatal and install the material-storage runtime dependencies explicitly in Dockerfile before requirements.txt, so a stale/mixed requirements file cannot block the image build.
